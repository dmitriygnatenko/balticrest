{% extends 'balticrest/base_left_top_menu.html.twig' %}

{% block title %}{{ parent() }} {% trans from 'seo' %}registration.title{% endtrans %}{% endblock %}
{% block description %}{% trans from 'seo' %}registration.desc{% endtrans %}{% endblock %}
{% block keywords %}{% trans from 'seo' %}registration.keywords{% endtrans %}{% endblock %}

{% trans_default_domain 'auth' %}

{% block body_attr %}class="innerpage"{% endblock %}

{% block content %}
    <div class="login_form">
        <h3>
            {% trans %}registration.title{% endtrans %}
            <span></span>
        </h3>

        {% if not confirm_email %}

            {{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}

                {% set form_errors = form.vars.errors.form.getErrors(true) %}

                {% if form_errors|length %}
                    <div class="errors">
                        {% for error in form_errors %}
                            {{ error.message|raw }}<br>
                        {% endfor %}
                    </div>
                {% endif %}

                <div class="form-group {% if form_errors(form.username) %}has-error{% endif %}">
                    <label>
                        {{ form.username.vars.label|trans }}:
                        {{ form_widget(form.username, {'attr': {'class': 'form-control'}}) }}
                    </label>
                </div>

                <div class="form-group {% if form_errors(form.email) %}has-error{% endif %}">
                    <label>
                        {{ form.email.vars.label|trans }}:
                        {{ form_widget(form.email, {'attr': {'class': 'form-control'}}) }}
                    </label>
                </div>

                <button class="btn btn-success" type="submit">
                    {% trans %}registration.form.button.register{% endtrans %}
                </button>

            {{ form_end(form) }}

        {% else %}

            <div class="success">
                {% trans with {'%email%':confirm_email} from 'auth' %}registration.confirm_text{% endtrans %}
            </div>

            <a href="{{ path('login') }}" class="btn btn-primary">
                {% trans %}registration.form.button.back{% endtrans %}
            </a>

        {% endif %}

    </div>
{% endblock %}
